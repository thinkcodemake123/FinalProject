import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class Instructiontext2 here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class Instructiontext2 extends Actor
{
    public Instructiontext2()
    {
        GreenfootImage image = new GreenfootImage("Prevent the rockets from destroying the buildings. Rocket does no damage to the cat.",20, Color.BLACK,null);
        setImage(image);
    }
}
